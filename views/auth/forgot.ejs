<!DOCTYPE html>
<html>
<head>
  <title>Forgot Password - DailyShop</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('../layout/header') %>
  <main class="container" style="padding:48px;display:flex;justify-content:center;">
    <div style="max-width:520px;background:#fff;padding:28px;border-radius:10px;box-shadow:0 12px 40px rgba(2,6,23,0.06);">
      <h2>Forgot Password</h2>
      <p>Enter your email and we'll generate a password reset link (check server logs in development).</p>
      <% if (error) { %>
        <div style="color:#ef4444;margin:10px 0;"><%= error %></div>
      <% } %>
      <% if (message) { %>
        <div style="color:#037d50;margin:10px 0; font-weight:600;"><%= message %></div>
      <% } %>
      <form action="/auth/forgot" method="POST">
        <label>Email</label>
        <input type="email" name="email" required style="width:100%;padding:10px;border-radius:6px;border:1px solid #e6e9ef;margin-top:6px;" />
        <div style="margin-top:12px;text-align:right;"><button type="submit" style="padding:10px 16px;border-radius:8px;background:#06b6d4;color:#fff;border:none;">Send Reset Link</button></div>
      </form>
    </div>
  </main>
  <%- include('../layout/footer') %>
</body>
</html>