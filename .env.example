# Copy this file to .env and fill in values
# Example MongoDB Atlas connection string (replace <username>, <password>, and <cluster-url>):
# MONGO_URI="mongodb+srv://<username>:<password>@cluster0.abcd.mongodb.net/dailyshop?retryWrites=true&w=majority"

# Session secret for express-session
# SESSION_SECRET=your_long_random_secret_here

# Optional: PORT to run the server
# PORT=10000

MONGO_URI=your_mongodb_atlas_connection_string_here
PORT=10000
NODE_ENV=production
